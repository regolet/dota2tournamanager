{
  "name": "dota2-tournament-manager",
  "version": "2.0.0",
  "description": "Serverless Dota 2 Tournament Management System",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build needed'",
    "deploy": "netlify deploy --prod",
    "start": "netlify dev"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.0",
    "node-fetch": "^3.3.2",
    "bcrypt": "^5.1.1",
    "isomorphic-dompurify": "^2.15.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
